// 【問題: 単位の計算】
// ある正の整数nと距離の単位を表す文字列sがあります
// nをsに合わせてすべてmmに変換して出力してください
// sには「km」,「m」,「cm」の3種類が入力されます

func test(n: Int, s: String) {
    print(n)
}

// 入力例1: 「n: 1, s: "km"]」 出力例1: 「1000000」
// 入力例2: 「n: 2, s: "cm"]」 出力例2: 「20」
// 入力例3: 「n: 12, s: "m"]」 出力例3: 「12000」

test(n: 1, s: "km")
